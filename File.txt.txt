SELECT
    employee_id,first_name,last_name,salary
FROM (SELECT
        employee_id,first_name,last_name,salary,ROW_NUMBER() OVER (ORDER BY employee_id) AS rn
    FROM employee) AS ranked_employees WHERE rn = 5;
   